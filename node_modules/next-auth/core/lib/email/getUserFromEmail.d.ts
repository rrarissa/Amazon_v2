import type { InternalOptions } from "../../types";
export default function getUserFromEmail({ email, adapter, withId, }: {
    email: string;
    adapter: InternalOptions<"email">["adapter"];
    withId: boolean;
}): Promise<{
    id: string;
    email: string;
} | {
    email: string;
    id?: undefined;
}>;
