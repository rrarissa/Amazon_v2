"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getUserFromEmail;

async function getUserFromEmail({
  email,
  adapter,
  withId = false
}) {
  var _ref;

  const {
    getUserByEmail
  } = adapter;
  return ((_ref = email ? await getUserByEmail(email) : null) !== null && _ref !== void 0 ? _ref : withId) ? {
    id: email,
    email
  } : {
    email
  };
}